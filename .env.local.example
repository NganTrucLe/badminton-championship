# Copy to .env.local (gitignored) and fill in with `supabase status -o env` after `supabase start`.
# Only the anon key belongs here — never the service_role key, never in a NEXT_PUBLIC_* var.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
